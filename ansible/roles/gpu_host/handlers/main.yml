---
- name: Restart docker
  ansible.builtin.systemd:
    name: docker
    state: restarted

- name: Restart ollama proxy
  community.docker.docker_container:
    name: ollama-proxy
    state: started
    restart: true
