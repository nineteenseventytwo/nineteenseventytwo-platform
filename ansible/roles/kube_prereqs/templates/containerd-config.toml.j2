# Managed by ansible/roles/kube_prereqs. Do not edit on the host.
version = 2

[plugins."io.containerd.grpc.v1.cri"]
  sandbox_image = "registry.k8s.io/pause:3.10"

  [plugins."io.containerd.grpc.v1.cri".containerd]
    default_runtime_name = "runc"

    [plugins."io.containerd.grpc.v1.cri".containerd.runtimes.runc]
      runtime_type = "io.containerd.runc.v2"

      [plugins."io.containerd.grpc.v1.cri".containerd.runtimes.runc.options]
        # Must match the kubelet's cgroup driver. Ubuntu 24.04 is cgroup v2 and
        # the kubelet defaults to systemd; a mismatch here is the classic
        # "kubelet starts, pods never become ready" symptom.
        SystemdCgroup = true

  [plugins."io.containerd.grpc.v1.cri".registry]
    config_path = "/etc/containerd/certs.d"
